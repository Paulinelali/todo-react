import { useState } from "react";

const Navbar = () => {

  const [dropdown, setDropdown] = 
  console.log(useState(false));
  return (
    <nav>
      nav bar content
    </nav>
  );
};
export default Navbar;
