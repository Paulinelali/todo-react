import { useState } from "react";

const Navbar = () => {

  const [dropdown, setDr]
  console.log(useState(false));
  return (
    <nav>
      nav bar content
    </nav>
  );
};
export default Navbar;
